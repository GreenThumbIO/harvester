version: '3'
services:
  db:
    image: postgres
    env_file: .env
    volumes:
      - ./tmp/db:/var/lib/postgresql/data

  redis:
    image: redis

  web:
    build: .
    command: python3 manage.py runserver
    volumes:
      - .:/harvester
    ports:
      - "5000:5000"
    depends_on:
      - redis
      - db
